# Decision Log — ROSE Engine PRD

Canonical memory and audit trail. Every decision, change, and override is recorded here as the conversation unfolds.

## 2026-06-15 — Session start

- **Intent:** Create (no prior PRD found in `_bmad-output/planning-artifacts/prds/`).
- **Inputs provided by user:** `docs/SPEC.md` (dev spec, P0), plus `docs/` set: Rose Presentation_alternative story.pdf, Revealing the Soul of ROSE v1.0.docx, Understanding ROSE Through the Lens of a Living Cell.docx, ROSE PROJECT MEMO 20052026.pdf, ROSE - Maximizing The Commons Allocation's Leverage V1.docx, ROSE_Real_Time_Finance_Intro.docx. All extracted via subagents (digests held by parent).
- **Decision — PRD scope:** "ROSE Engine (the product)" — the financial infrastructure product (Note, ledger, trading, token issuance, Laplace platform upgrade), Engine-first per the documents' decided sequencing, with P0 as the near-term milestone. (Rejected: P0-only; whole-ROSE-system.)
- **Decision — Working mode:** Fast path (batch gaps → draft with [ASSUMPTION] tags → iterate).
- **Stakes (inferred, to confirm):** regulated, high-stakes financial product — real legal entities, licensed investment manager (Namara Wealth Advisors), production platform (Laplace / laplace.digital), multi-jurisdiction.
- **Open tension flagged to user:** altitude/evolution gap between SPEC.md (off-chain EUR/USD L=1 double-entry ledger) and Project Memo (Singapore VCC issuing tokenised Rose Notes embedding L/S perpetual tokens, CEX/DEX trading). Whether the off-chain ledger is the accounting foundation beneath the tokenised structure, or the model evolved, must be resolved.

## 2026-06-15 — Fast path gap answers (user)

- **A. SPEC↔Memo reconciliation:** EUR/USD **and BTC** are **P0 validation cases** for the coupled-coin model. The off-chain ledger spine + these pairs are the P0 validation/foundation layer; the target product issues Rose Notes on perpetuals. (Note: BTC added beyond SPEC's EUR/USD-only.)
- **B. Roadmap:** No other document exists at this stage. Roadmap beyond P0 derived from SPEC hints only → mark [ASSUMPTION].
- **C. Users & surfaces:** Confirmed 3 surfaces — Subscribers/Rose Members via **laplace.digital** marketplace, internal **covenant console**, the **investment manager (Namara)**. **KYC / onboarding: out of scope** for this phase.
- **D. Build approach:** **Greenfield** (inspired by Laplace, NOT an upgrade of the Laplace stack). Overrides the Memo's "upgrade the current platform stack" wording.
- **E. Success metrics:** (1) **Funds raised**; (2) **coupled-coin model works on a pair of coupled coins**; (3) **full lifecycle traversed** (PENDING→ACTIVE→REBALANCING/PARTIAL/SETTLING→CLOSED).
- **F. Parked parameters:** confirmed — Note coupon, use-of-proceeds split, conversion-to-participation, backing-float floor remain unset config; absence → explicit refusal, never a silent default.
- **G. PRD audience:** build teams + stakeholders/board.
- **Stakes:** confirmed high (regulated financial product) — user did not contest.

## 2026-06-15 — Draft review, point-by-point resolutions

- **Point 1 — Note layer depth in P0 = (b) MORE than accounting.** User wants **b1 + b2 + b3** all in P0: real subscription/redemption (b1), real strategy execution (b2), real on-chain token minting (b3).
- **Token standard:** coupled tokens MUST be **ERC-3643 (T-REX) permissioned security tokens on EVM**.
- **Implication — CONFLICT with SPEC.md, resolved:** This overrides three SPEC §0 non-objectives — "no EVM", "no smart contracts", "on-chain authorization = P3+". User confirms (Q2=yes) these are **superseded**; SPEC.md must be updated.
- **Structure confirmed (Q1=ok, reading (i)):** the **off-chain double-entry ledger spine remains the accounting foundation / source of truth**; the live bricks (b1/b2/b3) sit on top within the same P0/MVP milestone — the off-chain ledger is NOT discarded.
- **Architectural consequence:** the single authorization chokepoint (`AuthorizationProvider` → `OnChainPolicyProvider`, originally P3+) effectively arrives in **P0**, and can be embodied by **ERC-3643 compliance modules**. The Model-A segregation bright line becomes an **on-chain compliance rule**, not only application-level.
- **PRD edits pending (apply in integration pass):** §5 Non-Goals (remove no-EVM/no-smart-contracts/no-live-trading/no-token-minting for P0; keep KYC out per earlier — but ERC-3643 implies on-chain identity, revisit KYC), §6 MVP Scope (move b1/b2/b3 + ERC-3643 in-scope), §4.3 (on-chain provider now P0), §4.5 (live Note flow), §10/§12 (compliance), §17 Roadmap (collapse on-chain from P3+ into P0), §8 open Qs, addendum (ERC-3643/EVM stack).
- **New queue item added — Point 8:** KYC / on-chain identity tension vs ERC-3643's ONCHAINID eligibility requirement (user had said "KYC out of scope"; ERC-3643 needs verified holder identity).

- **Point 2 — BTC validation = (a) stress-test at L=1.** BTC stays at L=1 deliberately so it *exhibits* rebalancing (opposite of EUR/USD whose barrier is ~never hit). AND: **L must remain parameterizable per coupled-pair** in the system — never a hard-coded constant. Confirms FR-6 assumption → remove that `[ASSUMPTION]` tag. SM-C1 (low reset rate) applies to EUR/USD; BTC is expected to show resets — clarify in §7 so a high BTC reset rate is not misread as model failure.

- **Point 3 — crypto-leg ledger representation = (c) hybrid.** Ledger tracks **both value AND token quantities** (L-Token/S-Token + on-chain positions); reconciliation (§4.4) verifies **ledger ↔ chain consistency** (chain and ledger must agree). Integer-minor-unit rule (NFR-2) **extends to crypto** (token smallest unit / wei) — taken as confirmed unless user nuances. Replaces earlier `[ASSUMPTION]` (notional/cash-equivalent, custody later) → now real on-chain custody in P0. FR-10/FR-11 to update; add ledger↔chain reconciliation as an explicit consequence.

## 2026-06-15 — Finalize: reviews, triage, polish, close

- **Decision-log audit:** all prior decisions confirmed captured in PRD/addendum (table walked with user). Nothing orphaned.
- **Input reconciliation (5 subagents):** wrote `reconcile-{spec,memo,rtf,soul,vision}.md`. Key gaps folded in via Tier-2 autofix (below). SPEC load-bearing substance well preserved; intentional supersessions confirmed checked.
- **Reviewer gate (3 subagents):** `review-rubric.md` (strong on all 7 dimensions, 0 critical/0 high/1 medium/5 low — build-ready on form), `review-adversarial-general.md` (4 criticals on model/architecture substance), `review-domain-finance.md` (conditional; ERC-3643 coupling + Model-A-on-chain not feasible as written). Adversarial + domain converged on the same substantive issues.
- **Triage decisions (user):**
  - **D1 (Rose Note composition & reset loss-allocation) = RESOLVED 2026-06-16 (user): separate L/S, zero-sum directional.** L and S held separately, each holder directional; at a reset the losing-leg holder bears the locked loss, the winning-leg holder is counterparty (gain funded from pool K). Was DEFERRED (owner: architecture/product); resolved by user. Applied to §8 Q1, §4.2 [NOTE FOR PM], §4.7, §15; epics.md (D1 + Story 2.4 AC); architecture.md (D1). **D1a (post-reset mechanic) = RESOLVED 2026-06-16 (user): crystallised & withdrawable.** Each reset realizes the winner's gain (withdrawable cash) and settles the loser's loss, then both legs re-base to a fresh symmetric split of the residual pool (new neutral cycle, no carried P&L); each reset = a settlement boundary (cash movement + balanced settlement journal entry). Directional one-leg note shape = implementation, lands Epics 4–6; reset settlement = Epics 5–7. SM-2 still scoped to invariant only.
  - **D2 (Model-A on-chain feasibility) = custom ERC-3643-compatible contract (OpenZeppelin) with additional rules** covering Model-A principal/yield + pair coupling. Resolves domain CRITICAL-1 and on-chain-coupling HIGH. Applied to FR-18/FR-19, glossary, addendum A/C.
  - **D3 (ledger↔chain authority) = chain is source of truth; off-chain ledger corrected to chain on conflict.** Inverted prior "ledger = source of truth". Applied to §1, §4.1, §4.4/FR-10, NFR-3/NFR-9, glossary, §15, addendum A/C.
  - **Tier 2 = autofix all** (applied): floor m/g added to parked params (§11.2) + SM-C1 falsifiability rule + model risk named (§15); regulated-token capabilities added (FR-21 burn, FR-22 agent powers/transfer-agent); allowlist gated on off-chain KYC/AML (§2.2/§5/§12); entity rename ISSUER_EXCHANGE→COIN_ISSUER + VCC cash/NAV-only + exchange under TRADING_CO + inter-entity whole-package flows (FR-1/FR-8/§12/addendum F); restored jurisdiction (FR-1), journal description (FR-2), coupled-pair field types (addendum D); no-accidental-real-money guard (§11.3); Intrinsic Time concept (§1/§4.7), Oanda/Lykke precedent (§15), anti-overbuild + why-wide-P0-coherent (§1/§6), structural-coherence/demonstration framing (§7/§13/§16), contributor roster (§16); honesty framing on "live=test funds"; mechanical fixes (§12 assumption→§9 index, treasury glossary, FR-9 testable consequence, BTC failure condition, delta vs market neutral, reconcile cadence).
  - **Tier 3 = accepted/deferred:** SM-1 no target (board), NFR-7 no latency budget (directional), 5 JTBD (justified).
- **Open items triaged:** §8 has 7 open questions, each with an owner — Q1 (architecture/product, before build), Q2 (board), Q3 (legal/structuring), Q4 (ops/legal), Q5/Q6 (architecture), Q7 (quant/method). Remaining [ASSUMPTION]s indexed in §9, all non-blocking for a testnet/paper P0 except Q1 (deferred by user choice).
- **Polish:** manual structural + prose pass; FR-1..22 unique/contiguous, NFR-1..9, SM-1..4+C1/C2, UJ-1..5 verified; glossary drift checked (treasury defined, COIN_ISSUER consistent). doc_standards editorial skills available for a deeper pass if requested.
- **External handoffs:** none configured — skipped.
- **Close:** status → final; updated 2026-06-15.

## 2026-06-15 — Open-assumption iteration (post-integration)

- **Execution venue & network (FR-20) = testnet/paper.** P0 on-chain = **Sepolia testnet** (ERC-3643 mint, token flows); strategy execution = **paper/testnet** (no real CEX, no real capital). Implication: P0 is "live in code, test funds" — NOT real fundraising. Reframed **SM-1** (subscription capability proven on testnet/paper; real capital = post-P0 board-gated). Updated §1 vision, FR-18 (Sepolia), FR-20 (paper/testnet), §6.1/§6.2 (+real-capital/mainnet out of scope), §11.3 (P0 touches no real money), §12 (live regulatory exposure deferred), §14 (Sepolia dep, paper venue), §17 (P0 testnet; beyond=real capital/mainnet board-gated), acceptance criteria, Open Qs (removed venue/network Q; ERC-3643 chain resolved=Sepolia, claim-issuer operating model still open). This partially reconciles with SPEC's "no real money" spirit.

- **Surfaces (FR-14, UJ-5) = ALL FUNCTIONAL in P0.** No mockups. Covenant Console, Coupled-Pair view, Exchange/Trading, and Subscriber surfaces all read live data. Removed mockup framing from §4.6/FR-14, UJ-5, §6.1; dropped surface open-questions (old Q3/Q4); trimmed §9 assumptions. Only remaining throwaway artifact is the §4.7 validation library/simulator (not a surface).

## 2026-06-15 — Integration pass applied

- `prd.md` fully rewritten to apply Points 1-8: §0 supersession notice (SPEC governed by PRD); §1 vision reframed (live vertical slice); §2.2 non-users (allowlist not KYC); §3 glossary (+ERC-3643, ONCHAINID, claim issuer, compliance module, allowlist, L per-pair, hybrid ledger); §4.1 hybrid value+quantity; §4.2 L parameter; §4.3 +FR-19 on-chain compliance; §4.4/FR-10 ledger↔chain; §4.5 live subscription/execution/minting (+FR-18 mint, +FR-20 execute); §4.6 functional-or-mocked; §4.7 BTC stress-test note; §5 Non-Goals rewritten + supersession note; §6 MVP = live small-scale slice; §7 SM-1 no target + SM-C1 excludes BTC; §8 new open Qs; §9 assumptions index trimmed; §10 NFR-2 (BigInt/NUMERIC/scale/rounding), NFR-4 dual fail-closed, NFR-7 TS+Rust/Go, NFR-9 ledger↔chain; §12 ERC-3643 + jurisdictions TBD; §17 minimal roadmap; acceptance criteria expanded.
- `addendum.md`: §A stack (TS default + Rust/Go, BigInt/NUMERIC, ERC-3643 on-chain stack); §C on-chain compliance now P0.
- **Remaining open `[ASSUMPTION]`s:** functional-vs-mocked surfaces (FR-14), P0 execution venue/network (FR-20), Subscriber read-surfaces (UJ-5) → all in §8 Open Questions.

- **Point 8 — KYC/identity vs ERC-3643 = (a).** No self-service KYC funnel, BUT the **ERC-3643 identity/eligibility infrastructure IS in P0 scope**: ONCHAINID registry, a trusted **claim issuer**, **compliance modules** enforcing transfer restrictions. Eligibility granted via a **curated allowlist** of sophisticated clients (claims issued by a trusted operator), not self-serve KYC. The **Model-A segregation bright line becomes an on-chain compliance rule** (ties back to Point 1). PRD: update §5 Non-Goals (KYC self-service out; on-chain identity/compliance in), §2.2 Non-Users, §4.3/§4.5, §12 regulatory, glossary (ONCHAINID, claim issuer, compliance module, allowlist).

- **Point 7 — Offshore jurisdictions = NOT yet identified.** Trading Co. (Jurisdiction 1) and Coin Issuer Co. (Jurisdiction 2) undetermined. Keep "Jurisdiction 1/2 — TBD" in §12 + Open Question. Context only; does not block P0 build.

- **Point 6 — Language = polyglot.** **TypeScript (Node 20+) = default PROD language** (ledger, orchestration, web3/ERC-3643 integration, surfaces); rationale: native EVM/web3 ecosystem (viem/ethers, T-REX ERC-3643 tooling), native `BigInt` (aligns with Point 3b), mature ORM/migrations. **Rust or Go for performance-critical modules** (high-frequency finance hot paths: execution/matching, real-time pricing/rebalancing engine). **Supersedes SPEC's "pick one language, stick to it."** Feeds NFR-7 (real-time). PRD/addendum: addendum stack section to reflect TS-default + Rust/Go hot-path; keep two-regime (PROD/throwaway) discipline orthogonal to language.

- **Point 5 — Roadmap = (a) minimal honest.** No roadmap doc exists. Rewrite §17: **P0 = the MVP** (off-chain spine + real subscription/execution/ERC-3643 minting + model validation), and beyond it list only **directions actually present in source docs** — Intraday Money Market → global liquidity pool; multi-pair/multi-asset scaling; cross-jurisdiction (Cayman) reconciliation — **unphased, undated**, flagged "roadmap to be formalized." Sharpen §6 MVP boundary as the load-bearing part. Note: my earlier inferred "on-chain = P3+" roadmap is now caduc — on-chain/ERC-3643 is in the MVP.

- **Point 4 — SM-1 funds-raised target/date = NONE.** No target amount, no deadline. SM-1 stays an explicit placeholder "to be defined by the board"; P0 financing success reframed as **the capability to actually raise** (b1 subscription flow works end-to-end) rather than a numeric threshold. Remove the `[ASSUMPTION]` placeholder note, replace with explicit "board to define."

- **Point 3b — numeric representation decision (user, after rounding-limitation Q).** **Keep integers in smallest unit** (exact, no binary float). Use **`BigInt` in code**; **switch to `NUMERIC` (arbitrary precision) when `bigint`/int64 is insufficient** — notably 18-decimal ERC-3643/ERC-20 tokens (int64 caps ~9 tokens at 18 decimals). **Supersedes SPEC's `bigint` for `collateral_pool K` and `amount`.** Also acted: (a) store **decimals/scale per asset** (EUR=2, BTC=8, token=its `decimals()`); (b) **deterministic remainder/rounding policy** so posted integer amounts preserve `V_A+V_B=K` exactly (model math may compute in higher precision, postings stay exact integers). PRD edits: refine **NFR-2**; add rounding-policy requirement; addendum stack note (NUMERIC/BigInt). Open question (entiers vs fixed-point NUMERIC(38,18)) is **resolved** in favor of integers-with-NUMERIC-fallback — not left open.

## 2026-06-18 — Update: secondary-trading position layer (§4.8)

- **Intent:** Update. **Change signal:** `sprint-change-proposal-2026-06-18.md` (user named it authoritative). Extracted via subagent; PRD + decision log mapped via a second subagent (extract-don't-ingest).
- **Trigger:** The mock-faithful redesign (spec #5, Exchange terminal `docs/mocks/exchange.html`) demanded a perp-style *buy-a-naked-leg* flow (leverage, live mark, entry, per-position P&L, per-user positions table) that **conflicts with the atomic-coupling invariant** (single leg unrepresentable on/off-chain — FR-6, FR-18/FR-19/FR-21). Classified MAJOR (crosses PRD + architecture + a new epic), not a Quick Dev increment.
- **Anchor decision — Resolution Option C (off-chain synthetic position layer).** Chosen over A (order-book matching), B (market-maker/AMM), D (redefine contracts for single legs). Rationale: preserves the invariant; **deployed ERC-3643 coupling contracts unchanged → no re-audit**; lowest effort (~1 epic / 6 stories, off-chain TS, no Solidity change); reuses proven patterns (dual-write outbox/saga, reconcile-and-correct, `postTransfer` chokepoint, already-built terminal UI). **Non-negotiable guardrail:** no single-leg mint/burn/transfer anywhere; any future on-chain coupling change re-opens this decision toward Option D with a fresh audit + product-safety sign-off.
- **Conflict check vs prior decisions:** no real reversals. Three surface tensions confirmed as *clarifications, not contradictions* — (1) §5/§6.2 "no general trading venue" stands (Option C is not a venue/CLOB/AMM); (2) §14 "no live feed in P0" preserved (oracle port added, P0 adapter = CSV/testnet); (3) per-user position model routed through atomic pairs, invariant intact. Reinforces **D1/D1a** (the position layer is the natural home for the directional per-holder view).
- **User arbitrations (AskUserQuestion):**
  - **Leverage =** *modelled but pinned to 1x in P0.* `leverage` field exists in the position schema (forward-extensible); P0 forces 1x; terminal selector renders disabled/fixed-1x; leverage >1x is post-P0. (Rejected: exclude entirely; full leverage in P0.)
  - **Oracle port =** *in P0 with CSV/replay-or-testnet adapter* (no live OANDA/LMAX). (Rejected: port-only/mark-deferred; all-deferred.)
- **PRD edits applied (`prd.md`):** new **§4.8** feature group + origin note; **FR-23** (per-user position model, leverage pinned 1x), **FR-24** (oracle port + mark-to-market, CSV/testnet, never-fabricate), **FR-25** (open/close composes atomic subscribe/redeem), **FR-26** (position P&L API + Exchange-terminal wiring), **FR-27** (position↔pair reconciliation, chain authoritative). Forward-refs in §4.5/FR-20 and §4.6/FR-14. Scope clarifications in §5 + §6.1 + §6.2. Price-oracle dependency in §14. New §8 **Q8** (claim/assignment semantics → architecture). New §15 risks (synthetic-exposure/solvency w/ Lykke as review input; oracle risk; position-layer scope creep). Glossary: Position (synthetic), Price oracle, Mark-to-market. 4 new P0 acceptance criteria (FR-24/25/26/27). §9 resolved-list appended. Frontmatter `updated` → 2026-06-18.
- **Explicitly NOT changed (per guardrail):** FR-6, FR-18, FR-19, FR-21; atomic-coupling invariant; on-chain contracts; no re-audit. Single-leg remains impossible everywhere.
- **Open / deferred:** §8 Q8 — position claim/assignment semantics (assignment, partial fill, ordering) → architecture (Option C design). Real-feed/real-capital secondary market → board-gated (§11.3).
- **Downstream impact (not this artifact):** architecture.md (new "Secondary-Trading Position Layer (Option C)" decision; `prod/packages/positions` + `prod/packages/price-oracle`; endpoints; FR→structure mapping past FR-22); epics.md (new **Epic 8**, ~6 stories 8.1–8.6); UX (behavioral-wiring delta only — live marks/positions into existing terminal components, no new visual design). To be carried by `bmad-create-architecture` / `bmad-create-epics-and-stories` / `bmad-ux`.

### Finalize — Reviewer gate + triage (2026-06-18)

- **Reviewer gate (2 parallel subagents):** `review-rubric-2026-06-18.md` (**PASS-WITH-FIXES** — 0 critical/0 high; delta internally consistent, FR-23–27 contiguous, forward-refs resolve, glossary used) and `review-domain-finance-2026-06-18.md` (adversarial — **3 CRITICAL + 2 HIGH** on substance).
- **User decision on the phase-blocker (CRITICAL-1 + CRITICAL-2 = counterparty/inventory model): "Defer but bound."** Do not pick matched-book (A) vs house-inventory (B) now; instead **requalify §8 Q8** from "architecture mechanism detail" to a **BLOCKING solvency decision** (must be chosen + shown solvency-preserving before Epic 8 build), and add a binding PRD safety floor. Applied: §8 Q8 rewritten; new **§11.4 Position-layer solvency guardrail** (no unbacked leg / no synthetic naked exposure; per-pair/per-side solvency vs residual pool; Lykke a named review input); FR-25 consequence (independent close ≠ forced whole-package burn; gated by §11.4 until model chosen).
- **Autofixes applied (no new product decision):**
  - **CRITICAL-3 (FR-27):** rewrote to a **per-pair, per-side residual-backing invariant** (not gross issued notional netted across pairs/sides); added "correction touching a user position is journaled, never silent."
  - **HIGH-1 (position at reset):** FR-23 now enumerates position lifecycle `OPEN → (RESET) → CLOSED`, with `RESET` = D1/D1a settlement boundary (entry re-anchors to new P₀, P&L crystallises/withdrawable, re-base). New acceptance criterion.
  - **HIGH-2 (regulatory):** §12 assumption added flagging the position layer as CFD/derivative-like, for counsel.
  - **MEDIUMs:** FR-23 leverage-≠1-rejected acceptance criterion; FR-27 wired into **SM-4**; position lifecycle states enumerated; FR-26 "decimal strings = serialization at API boundary, not storage" (NFR-2 intact); FR-24 oracle **staleness/provenance + price-consistency** consequence + stale-mark acceptance criterion; §15 synthetic-exposure risk rewritten ("invariant necessary but not sufficient").
  - **LOW:** perp-terminal framing already covered by the §4.8 origin note.
- **Reviews retained** at `review-rubric-2026-06-18.md` / `review-domain-finance-2026-06-18.md`.
- **Polish:** manual structural + prose pass on §4.8 and touched sections; FR-1..27 contiguous, NFR-1..9, SM-1..4+C1/C2, UJ-1..5; glossary drift checked (Position/Price oracle/Mark-to-market defined and used).
- **Close:** status remains `final`; `updated` = 2026-06-18. External handoffs: none configured.

### Alpha Engine PoC — gap analysis + PRD cross-reference (2026-06-18)

- **Input:** `docs/alpha_engine_poc_v1.pdf` (*The Alpha Engine — POC Specification v1.0*). User asked for analysis + gap analysis vs ROSE; comparison baseline = PRD (§1 vision / §4.7 / §17).
- **Finding:** the PoC is a **closed agent-based market sim** (two fixed long/short agent populations; endogenous price via Dutch auction; carry-pressure firing; Pareto capital; agent mortality; zero-sum, P&L untracked). It is **a different simulation from ROSE's §4.7 coupled-coin validation** — endogenous vs exogenous price, agent populations vs one coupled instrument, "interesting emergent behaviour" vs invariant-proof. **It does not validate, replace, or advance any P0 requirement** and is **not** the §4.7 simulator.
- **User intent (Q4) = (b):** the PoC is an **alternative validation of the *intrinsic-time* thesis** (not primarily an Intraday-Money-Market brick). Reframed accordingly: §4.7 = intrinsic time at the *instrument/micro* level (threshold-only reset); Alpha Engine = intrinsic time at the *market/macro* level (carry-pressure firing + emergent price). Complementary lenses.
- **Decisions acted (user "OK pour reco"):** (1) classify the PoC as **Throwaway / R&D** — `/prod` never depends on it (§11.1); its binary `float` is tolerable only at that status; (2) anchor it to the intrinsic-time thread, **not P0**; (3) two conceptual reconciliations left open (could emergent `p_int` feed the coupled-coin model? is carry-pressure firing formally the same principle as floor-breach reset?).
- **Artifact written:** `_bmad-output/implementation-artifacts/gap-analysis-alpha-engine-poc-2026-06-18.md` (full synthesis, both-direction gap tables, intrinsic-time reframing).
- **PRD cross-reference APPLIED (user-confirmed "oui"):** §1 vision parenthetical; §4.7 "Related exploration (Throwaway, not P0)" note; §17 "Intrinsic-time research thread" direction. All classify the PoC as Throwaway, macro-level intrinsic-time validation, disjoint from P0, future link undecided. PRD stays `final`, `updated` 2026-06-18.
